# FastAPI Backend Requirements
# Pinned to exact versions to prevent future breakage.

# =============================================================================
# FastAPI
# =============================================================================
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.12.5
pydantic-settings==2.11.0

# =============================================================================
# AgentCore SDK (replaces in-memory agents)
# =============================================================================
# AgentCore Memory accessed via boto3 bedrock-agentcore client (no separate SDK)
# AgentCore CLI toolkit — provides the `agentcore` command used by deploy.sh
# to configure and deploy runtimes. Pinned to match the tested deployment.
bedrock-agentcore-starter-toolkit==0.3.2

# =============================================================================
# AWS SDK
# =============================================================================
boto3==1.42.63

# =============================================================================
# Authentication (existing)
# =============================================================================
PyJWT[crypto]==2.12.0
python-multipart==0.0.20

# =============================================================================
# Utils
# =============================================================================
requests==2.32.5
python-dotenv==1.2.1
httpx==0.28.1
python-json-logger==2.0.7
slowapi==0.1.9

# =============================================================================
# Agent Dependencies - REMOVED (agents run in AgentCore Runtime)
# =============================================================================
# The following dependencies are NO LONGER needed in backend:
# - mcp (runs in Runtime container)
#
# Backend now invokes agents via boto3.invoke_agent_runtime()
# See: agents/runtime/requirements.txt for Runtime container deps

# Keep parsedatetime for date parsing utilities in backend
parsedatetime==2.6

# =============================================================================
# CORS
# =============================================================================
fastapi-cors==0.0.6

# =============================================================================
# Lambda deployment (optional)
# =============================================================================
mangum==0.21.0
# LangGraph (in-process supervisor)
langgraph==0.5.4
langgraph-prebuilt==0.5.2
langchain-aws==0.2.17
langchain-core==0.3.86
openpyxl==3.1.2

# =============================================================================
# RCA Database Agent
# =============================================================================
psycopg2-binary==2.9.9
langchain-community==0.3.21
sqlalchemy==2.0.36

# =============================================================================
# OpenTelemetry (tracing & observability)
# =============================================================================
opentelemetry-api==1.30.0
opentelemetry-sdk==1.30.0
opentelemetry-exporter-otlp-proto-http==1.30.0
opentelemetry-instrumentation-fastapi==0.51b0
opentelemetry-instrumentation-httpx==0.51b0
opentelemetry-instrumentation-botocore==0.51b0
opentelemetry-instrumentation-requests==0.51b0

# =============================================================================
# Testing
# =============================================================================
pytest==8.3.4
pytest-asyncio==0.24.0
