# AgentCore deployment config for dev_aws_api_mcp
# Used by scripts/deploy.sh and the AgentCore SDK/CLI build flow.
runtime:
  name: dev_aws_api_mcp
  protocol: MCP
  runtime: PYTHON_3_11
  entrypoint: agents/aws_api_mcp/aws_api_mcp
  network_mode: PUBLIC          # matches deployed state; consider VPC for private tools
  lifecycle:
    idle_session_timeout: 900
    max_lifetime: 28800

artifact:
  s3_bucket: bedrock-agentcore-codebuild-sources-001961766007-us-east-1
  s3_prefix: dev_aws_api_mcp/deployment.zip

environment:
  BEDROCK_AGENTCORE_MEMORY_ID: dev_aws_api_mcp_mem-F4oSyTDTO0
  BEDROCK_AGENTCORE_MEMORY_NAME: dev_aws_api_mcp_mem

authorizer:
  type: CUSTOM_JWT
  discovery_url: https://cognito-idp.us-east-1.amazonaws.com/us-east-1_vyWBkPHjd/.well-known/openid-configuration
  allowed_clients:
    - 2t0feausc9empnuqq6lgns29bu
  allowed_scopes:
    - mcp-server/invoke

observability:
  otel: true                    # traces to X-Ray / Application Signals
  cloudwatch_logs: true
