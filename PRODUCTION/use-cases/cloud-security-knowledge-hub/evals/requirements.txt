# Eval harness dependencies
ragas>=0.2                    # RAG evaluation metrics
datasets>=2.19               # RAGAS input format
boto3>=1.35.0                # Bedrock LLM/embeddings for RAGAS + CloudWatch emit

# Dev / test only
pytest>=8.0
